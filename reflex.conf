+ Reflex config for local development.
+ Rebuild and restart the app when Go, template, CSS, JS, or image assets change.

-r '\.go$' -r '^templates/.*\.html$' -r '^static/.*\.(css|js|png|jpg|jpeg|webp|svg)$' -R '(^|/)\.(git|DS_Store)(/|$)' -s -- sh -c 'go run .'
